version: "2"
sql:
  - schema: "db/migrations"
    queries: "config/query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "db"
        sql_package: "pgx/v5" # Best for Postgres
        emit_json_tags: true # Best for React/JSON APIs
